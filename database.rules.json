{
  "rules": {
    "users": {
      "$uid": {
        ".write": "auth.uid === $uid",
        ".read": "auth.uid !== null"
      }
    },
    "bases": {
      "$uid": {
        ".write": "auth.uid === $uid",
        ".read": "auth.uid !== null"
      }
    },
    "travels": {
      "$uid": {
        ".write": "auth.uid === $uid",
        ".read": "auth.uid !== null"
      }
    }
  }
}
